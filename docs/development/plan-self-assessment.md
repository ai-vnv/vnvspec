# Wave A Plan Self-Assessment

This report was auto-generated by `scripts/wave_a_plan_assessment.py`.
It treats each build-plan phase goal as a synthetic Requirement
and runs the INCOSE GtWR quality checker against it.

## Results

### PLAN-PHASE-01

**Statement:** Empty but correctly wired Python 3.11+ package that passes `just check`.

**Violations (2):**

  - **R6 Unit-bearing** (warning): Statement contains numeric(s) ['3.11'] without recognized units. Add units to avoid ambiguity.
  - **R7 Complete** (error): Statement lacks both a subject (e.g., 'The system') and 'shall' language. A complete requirement identifies who/what and uses 'shall'.

### PLAN-PHASE-02

**Statement:** Define the Pydantic v2 models for the spec language. All domain-agnostic.

**Violations (1):**

  - **R7 Complete** (error): Statement lacks both a subject (e.g., 'The system') and 'shall' language. A complete requirement identifies who/what and uses 'shall'.

### PLAN-PHASE-03

**Statement:** `Requirement.check_quality()` returns a list of `RuleViolation` objects.

**Violations (1):**

  - **R7 Complete** (error): Statement lacks both a subject (e.g., 'The system') and 'shall' language. A complete requirement identifies who/what and uses 'shall'.

### PLAN-PHASE-04

**Statement:** `Spec.trace_graph()` returns a directed acyclic graph of links between requirements, hazards, evidence, ODDs.

**Violations (1):**

  - **R7 Complete** (error): Statement lacks both a subject (e.g., 'The system') and 'shall' language. A complete requirement identifies who/what and uses 'shall'.

### PLAN-PHASE-05

**Statement:** `vnvspec.registries.load("iso_pas_8800")` returns a typed registry object; 5 bundles shipped.

**Violations (2):**

  - **R6 Unit-bearing** (warning): Statement contains numeric(s) ['5'] without recognized units. Add units to avoid ambiguity.
  - **R7 Complete** (error): Statement lacks both a subject (e.g., 'The system') and 'shall' language. A complete requirement identifies who/what and uses 'shall'.

### PLAN-PHASE-06

**Statement:** `IOContract` can be backed by pydantic for records or pandera for dataframes.

**Violations (2):**

  - **R2 Singular** (warning): Statement contains conjunction(s): ['']. Consider splitting into separate requirements.
  - **R7 Complete** (error): Statement lacks both a subject (e.g., 'The system') and 'shall' language. A complete requirement identifies who/what and uses 'shall'.

### PLAN-PHASE-07

**Statement:** Publish the pluggability contract (ModelAdapter, TestRunner, Exporter protocols).

**Violations (1):**

  - **R7 Complete** (error): Statement lacks both a subject (e.g., 'The system') and 'shall' language. A complete requirement identifies who/what and uses 'shall'.

### PLAN-PHASE-08

**Statement:** `vnvspec.torch.TorchAdapter` wraps any `nn.Module` and implements `ModelAdapter`.

**Violations (2):**

  - **R2 Singular** (warning): Statement contains conjunction(s): ['']. Consider splitting into separate requirements.
  - **R7 Complete** (error): Statement lacks both a subject (e.g., 'The system') and 'shall' language. A complete requirement identifies who/what and uses 'shall'.

### PLAN-PHASE-09

**Statement:** `vnvspec.torch.TransformerAdapter` wraps HF `transformers` encoder and seq2seq models.

**Violations (2):**

  - **R2 Singular** (warning): Statement contains conjunction(s): ['']. Consider splitting into separate requirements.
  - **R7 Complete** (error): Statement lacks both a subject (e.g., 'The system') and 'shall' language. A complete requirement identifies who/what and uses 'shall'.

### PLAN-PHASE-10

**Statement:** `AutoregressiveAdapter` and `VLMAdapter`.

**Violations (2):**

  - **R2 Singular** (warning): Statement contains conjunction(s): ['']. Consider splitting into separate requirements.
  - **R7 Complete** (error): Statement lacks both a subject (e.g., 'The system') and 'shall' language. A complete requirement identifies who/what and uses 'shall'.

### PLAN-PHASE-11

**Statement:** `spec.generate_tests()` emits a pytest file; `spec.generate_hypothesis()` emits property tests.

**Violations (1):**

  - **R7 Complete** (error): Statement lacks both a subject (e.g., 'The system') and 'shall' language. A complete requirement identifies who/what and uses 'shall'.

### PLAN-PHASE-12

**Statement:** Four export formats (HTML, Markdown, GSN Mermaid, Annex IV techdoc).

**Violations (1):**

  - **R7 Complete** (error): Statement lacks both a subject (e.g., 'The system') and 'shall' language. A complete requirement identifies who/what and uses 'shall'.

### PLAN-PHASE-13

**Statement:** `vnvspec` CLI command.

**Violations (1):**

  - **R7 Complete** (error): Statement lacks both a subject (e.g., 'The system') and 'shall' language. A complete requirement identifies who/what and uses 'shall'.

### PLAN-PHASE-14

**Statement:** MkDocs Material site builds cleanly.

**Violations (1):**

  - **R7 Complete** (error): Statement lacks both a subject (e.g., 'The system') and 'shall' language. A complete requirement identifies who/what and uses 'shall'.

### PLAN-PHASE-15

**Statement:** `examples/` each run standalone; `tests/integration/` pass.

**Violations (1):**

  - **R7 Complete** (error): Statement lacks both a subject (e.g., 'The system') and 'shall' language. A complete requirement identifies who/what and uses 'shall'.

### PLAN-PHASE-16

**Statement:** A releasable wheel.

**Violations (1):**

  - **R7 Complete** (error): Statement lacks both a subject (e.g., 'The system') and 'shall' language. A complete requirement identifies who/what and uses 'shall'.

## Summary

- **Total phases:** 16
- **Phases with violations:** 16
- **Total violations:** 22

Most violations stem from the fact that build-plan goals are
informal milestones, not engineering requirements. They lack
'shall' language, clear subjects, and are often compound.
This is expected and demonstrates that the GtWR checker
correctly flags non-requirement prose.
